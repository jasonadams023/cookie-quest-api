version: '3.4'

services:
  api:
    build:
      context: .
      dockerfile: ./docker/multi/Dockerfile
      target: test
    image: 'cookie-quest-api:multi'
    volumes:
      - ./:/cookie-quest
    ports:
      - '3000:3000'
    command: npm run start:watch
